spring:
  application:
    name: api-nida

  datasource:
    url: jdbc:postgresql://localhost:5432/nida
    username: postgres
    password: 2002
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect